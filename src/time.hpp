#include "Utils.hpp"

namespace time {
    float dTime = 0.0f;
    float lastFrame = 0.0f;
    float curFrame = 0.0f;
    int framerate = 0;

    static inline void get_time()
    {

    }

    static inline void update() 
    {

    }
};
